/* ===== Nail Mania data: categories, products, brands, i18n (RO/RU) ===== */

// ---- UI strings ----
export const I18N = {
  ro:{
    topPromo:"Livrare gratuită prin toată Moldova de la 1800 lei",
    hours:"Zilnic 09:00–18:00",
    catalog:"Catalog", searchPh:"Caută produse, branduri…", search:"Caută",
    fav:"Favorite", cart:"Coș", menu:"Meniu",
    navNew:"Noutăți", navSale:"Reduceri", navBrands:"Branduri", navDelivery:"Livrare", navContact:"Contacte",
    heroKicker:"Colecția de sezon", heroTitle:"Trendurile\nsezonului", heroText:"Geluri, baze și topuri pentru maeștrii manichiurii — totul într-un singur loc.",
    heroBtn:"Vezi catalogul", heroBtn2:"Noutăți",
    pillCatalog:"Catalog", pillNew:"Noutăți", pillSale:"Reduceri",
    catsTitle:"Categorii de produse",
    secBest:"Hituri de vânzări", secNew:"Noi sosiri la Nail Mania", secSale:"Produse la reducere",
    all:"Toate", addCart:"În coș", inCart:"În coș ✓", quickAdd:"Adaugă rapid",
    brandsTitle:"Brandurile noastre", brandsSub:"Lucrăm doar cu producători de încredere",
    allBrands:"Toate brandurile", filterBrand:"Brand",
    allCats:"Toate categoriile", filterCat:"Categorie",
    deliveryTitle:"Livrarea produselor", deliverySub:"de la Nail Mania",
    d1:"Ridicare personală din magazin – gratuit.",
    d2:"Livrare în Ungheni – 50 lei sau gratuit de la 350 lei.",
    d3:"Curier prin Moldova – de la 60 lei, gratuit de la 2000 lei.",
    d4:"Livrare în Europa – stabilită individual cu managerul.",
    termsTitle:"Termene de livrare",
    t1:"Ridicare personală – în decurs de 1 oră (până la 18:00).",
    t2:"Livrare în Ungheni – în decurs de 2 ore.",
    t3:"Livrare în Moldova – 1–3 zile lucrătoare.",
    contact:"Contacte", emptyCart:"Coșul tău este gol", emptyFav:"Nu ai produse favorite",
    total:"Total", checkout:"Finalizează comanda", clear:"Golește", goShop:"La cumpărături",
    footAbout:"Magazin pentru maeștrii manichiurii și pedichiurii din Moldova.",
    colCatalog:"Catalog", colInfo:"Informații", colContact:"Contacte",
    newBadge:"NOU", saleBadge:"-", off:"reducere",
    results:"rezultate", noResults:"Nimic găsit", featured:"Recomandat",
    addedToast:"Adăugat în coș", lei:"lei",
    menuTitle:"Meniu", langLabel:"Limba",
    // ---- product page ----
    home:"Acasă", inStock:"În stoc", outOfStock:"Stoc epuizat", code:"Cod", qty:"Cantitate",
    buyOneClick:"Cumpără într-un clic", goCheckout:"Spre finalizare",
    descTitle:"Descriere", charTitle:"Caracteristici",
    charBrand:"Brand", charCat:"Categorie", charCode:"Cod", charAvail:"Disponibilitate",
    relatedTitle:"Produse similare", notFound:"Produsul nu a fost găsit", backHome:"Înapoi la magazin",
    descA:"de la {brand} — produs profesional din categoria „{cat}”, creat pentru maeștrii manichiurii și pedichiurii. Calitate înaltă, rezultat durabil și aplicare ușoară.",
    descB:"Formulă testată în salon, ambalaj comod și consum economic. Comandă acum și ridică din magazin sau prin curier în toată Moldova.",
    // ---- checkout ----
    checkoutTitle:"Finalizarea comenzii", deliverySection:"Livrare",
    chooseDelivery:"Alege metoda de livrare",
    courier:"Curier", courierDesc:"Livrare la adresă prin toată Moldova",
    pickup:"Ridicare din magazin", pickupDesc:"str. Romană 66/2, Ungheni — gratuit",
    recipient:"Destinatar", fullName:"Nume și Prenume", phone:"Telefon mobil",
    emailLabel:"Email", addressLabel:"Adresa de livrare", cityLabel:"Oraș / Localitate",
    commentLabel:"Comentariu", commentPh:"Comentariu la comandă (opțional)",
    paymentSection:"Metoda de plată",
    payMia:"MIA", payMiaDesc:"Plată instant prin aplicația MIA",
    payCard:"Transfer pe card", payCardDesc:"Transfer bancar pe card",
    payCash:"Numerar la primire", payCashDesc:"Plătești la curier sau în magazin",
    yourOrder:"Comanda ta", itemsLabel:"Produse", discountLabel:"Reducere",
    deliveryLabel:"Livrare", freeLabel:"Gratuit",
    agreePre:"Sunt de acord cu termenii", agreeLink:"acordului de utilizare",
    placeOrder:"Finalizează comanda", emptyCheckout:"Coșul tău este gol",
    orderSuccess:"Comanda a fost plasată!",
    orderSuccessText:"Vă mulțumim! Un manager vă va contacta în curând pentru confirmare.",
    orderNo:"Numărul comenzii", continueShopping:"Continuă cumpărăturile",
    reqField:"Câmp obligatoriu", reqDelivery:"Alege metoda de livrare", reqPayment:"Alege metoda de plată"
  },
  ru:{
    topPromo:"Бесплатная доставка по Молдове от 1800 лей",
    hours:"Ежедневно 09:00–18:00",
    catalog:"Каталог", searchPh:"Поиск товаров, брендов…", search:"Найти",
    fav:"Избранное", cart:"Корзина", menu:"Меню",
    navNew:"Новинки", navSale:"Скидки", navBrands:"Бренды", navDelivery:"Доставка", navContact:"Контакты",
    heroKicker:"Коллекция сезона", heroTitle:"Тренды\nсезона", heroText:"Гель-лаки, базы и топы для мастеров маникюра — всё в одном месте.",
    heroBtn:"Смотреть каталог", heroBtn2:"Новинки",
    pillCatalog:"Каталог", pillNew:"Новинки", pillSale:"Скидки",
    catsTitle:"Категории товаров",
    secBest:"Хиты продаж", secNew:"Новое поступление в Nail Mania", secSale:"Товары со скидкой",
    all:"Все", addCart:"В корзину", inCart:"В корзине ✓", quickAdd:"Быстрый заказ",
    brandsTitle:"Наши бренды", brandsSub:"Работаем только с проверенными производителями",
    allBrands:"Все бренды", filterBrand:"Бренд",
    allCats:"Все категории", filterCat:"Категория",
    deliveryTitle:"Доставка товаров", deliverySub:"от Nail Mania",
    d1:"Самовывоз из магазина – бесплатно.",
    d2:"Доставка по Унгенам – 50 лей или бесплатно от 350 лей.",
    d3:"Курьером по Молдове – от 60 лей, бесплатно от 2000 лей.",
    d4:"Доставка в Европу – по договорённости с менеджером.",
    termsTitle:"Сроки доставки",
    t1:"Самовывоз – в течение 1 часа (до 18:00).",
    t2:"Доставка по Унгенам – в течение 2 часов.",
    t3:"Доставка по Молдове – 1–3 рабочих дня.",
    contact:"Контакты", emptyCart:"Ваша корзина пуста", emptyFav:"Нет избранных товаров",
    total:"Итого", checkout:"Оформить заказ", clear:"Очистить", goShop:"За покупками",
    footAbout:"Магазин для мастеров маникюра и педикюра в Молдове.",
    colCatalog:"Каталог", colInfo:"Информация", colContact:"Контакты",
    newBadge:"НОВОЕ", saleBadge:"-", off:"скидка",
    results:"результатов", noResults:"Ничего не найдено", featured:"Рекомендуем",
    addedToast:"Добавлено в корзину", lei:"лей",
    menuTitle:"Меню", langLabel:"Язык",
    // ---- product page ----
    home:"Главная", inStock:"В наличии", outOfStock:"Нет в наличии", code:"Артикул", qty:"Количество",
    buyOneClick:"Купить в один клик", goCheckout:"К оформлению",
    descTitle:"Описание", charTitle:"Характеристики",
    charBrand:"Бренд", charCat:"Категория", charCode:"Артикул", charAvail:"Наличие",
    relatedTitle:"Похожие товары", notFound:"Товар не найден", backHome:"Назад в магазин",
    descA:"от {brand} — профессиональный продукт из категории «{cat}», созданный для мастеров маникюра и педикюра. Высокое качество, стойкий результат и лёгкое нанесение.",
    descB:"Формула, проверенная в салоне, удобная упаковка и экономичный расход. Закажите сейчас и заберите из магазина или курьером по всей Молдове.",
    // ---- checkout ----
    checkoutTitle:"Оформление заказа", deliverySection:"Доставка",
    chooseDelivery:"Выберите способ доставки",
    courier:"Курьер", courierDesc:"Доставка по адресу по всей Молдове",
    pickup:"Самовывоз из магазина", pickupDesc:"ул. Романэ 66/2, Унгены — бесплатно",
    recipient:"Получатель", fullName:"Имя и Фамилия", phone:"Мобильный телефон",
    emailLabel:"Электронная почта", addressLabel:"Адрес доставки", cityLabel:"Город / Населённый пункт",
    commentLabel:"Комментарий", commentPh:"Комментарий к заказу (необязательно)",
    paymentSection:"Способ оплаты",
    payMia:"MIA", payMiaDesc:"Мгновенная оплата через приложение MIA",
    payCard:"Перевод на карту", payCardDesc:"Банковский перевод на карту",
    payCash:"Наличными при получении", payCashDesc:"Оплата курьеру или в магазине",
    yourOrder:"Ваш заказ", itemsLabel:"Товары", discountLabel:"Скидка",
    deliveryLabel:"Доставка", freeLabel:"Бесплатно",
    agreePre:"Я согласен с условиями", agreeLink:"пользовательского соглашения",
    placeOrder:"Оформить заказ", emptyCheckout:"Ваша корзина пуста",
    orderSuccess:"Заказ оформлен!",
    orderSuccessText:"Спасибо! Менеджер свяжется с вами в ближайшее время для подтверждения.",
    orderNo:"Номер заказа", continueShopping:"Продолжить покупки",
    reqField:"Обязательное поле", reqDelivery:"Выберите способ доставки", reqPayment:"Выберите способ оплаты"
  }
};

// ---- Categories ----
import CATEGORIES_RAW from './categories.json';
// Curated metadata (nice bilingual names, gradients, icons). The client can add
// brand-new categories straight in the spreadsheet — those appear automatically
// with default styling; add an entry here only to give one a custom name/icon.
const CURATED_CATS = [
  {id:"gellac", ro:"Gel Lac", ru:"Гель-лак", g:["#e8c6d4","#f3e0e7"], icon:"bottle"},
  {id:"baze", ro:"Baze", ru:"Базы", g:["#f4ddd6","#fbeee9"], icon:"bottle"},
  {id:"topuri", ro:"Topuri", ru:"Топы", g:["#e7d3e6","#f6eaf4"], icon:"bottle"},
  {id:"alungire", ro:"Alungire", ru:"Наращивание", g:["#dcc7e6","#efe2f4"], icon:"tip"},
  {id:"solutii", ro:"Soluții pregătitoare", ru:"Подготовка", g:["#cfd9e6","#e9eef4"], icon:"bottle"},
  {id:"bituri", ro:"Bituri", ru:"Фрезы", g:["#d9d9de","#eeeef0"], icon:"bit"},
  {id:"instrumente", ro:"Instrumente", ru:"Инструменты", g:["#d3d6da","#ebecef"], icon:"tool"},
  {id:"lichide", ro:"Lichide", ru:"Жидкости", g:["#cfe0e0","#e8f2f2"], icon:"bottle"},
  {id:"sterilizare", ro:"Sterilizare & Dezinfectare", ru:"Стерилизация", g:["#c9dbe8","#e6eef5"], icon:"box"},
  {id:"epilare", ro:"Epilare", ru:"Эпиляция", g:["#f0e3c8","#f9f1de"], icon:"box"},
  {id:"pedichiura", ro:"Totul pentru pedichiură", ru:"Всё для педикюра", g:["#e9cdbf","#f6e4da"], icon:"foot"},
  {id:"tehnica", ro:"Tehnica", ru:"Техника", g:["#dcd2e0","#efe9f2"], icon:"lamp"},
  {id:"accesuare", ro:"Accesuare pentru meșteri", ru:"Аксессуары", g:["#e7d6dd","#f5ebef"], icon:"box"},
  {id:"sprancene", ro:"Totul pentru sprâncene", ru:"Для бровей", g:["#e4d3cb","#f3e7e1"], icon:"brow"},
  {id:"gene", ro:"Totul pentru gene", ru:"Для ресниц", g:["#ddd0cf","#f0e7e6"], icon:"brow"},
  {id:"ingrijire", ro:"Îngrijire", ru:"Уход", g:["#f0dce2","#fbeef2"], icon:"bottle"},
  {id:"materiale", ro:"Materiale de unică folosință", ru:"Одноразовые", g:["#dfe0e2","#f1f1f3"], icon:"box"},
  {id:"design", ro:"Design", ru:"Дизайн", g:["#e7c9dd","#f6e7f1"], icon:"sparkle"},
  {id:"slidiz", ro:"Slidere", ru:"Слайдеры", g:["#cfe0ea","#e9f1f7"], icon:"sparkle"}
];
// auto-styling for categories that aren't curated above
const CAT_PALETTE = [["#e8c6d4","#f3e0e7"],["#cfe0e0","#e8f2f2"],["#e7d3e6","#f6eaf4"],["#f0e3c8","#f9f1de"],["#cfd9e6","#e9eef4"],["#e9cdbf","#f6e4da"],["#dfe0e2","#f1f1f3"],["#ddd0cf","#f0e7e6"]];
const _hh = (s)=>{ let h=0; for(let i=0;i<s.length;i++) h=(h*31+s.charCodeAt(i))>>>0; return h; };
const _curatedById = Object.fromEntries(CURATED_CATS.map(c=>[c.id,c]));
// Final list: only categories that actually have products (curated order first),
// then any new ones from the sheet, styled automatically.
export const CATS = [
  ...CURATED_CATS.filter(c=> CATEGORIES_RAW.some(x=>x.id===c.id)),
  ...CATEGORIES_RAW.filter(c=> !_curatedById[c.id]).map(c=>({
    id:c.id, ro:c.label, ru:c.label, g:CAT_PALETTE[_hh(c.id)%CAT_PALETTE.length], icon:"box",
  })),
];

// ---- Products come from the real price list (see CATALOG below) ----

// ---- Local images ----
// Drop your own photos into public/images/ (see public/images/README.md for the
// exact filenames). Any image you haven't added yet falls back to the gradient
// automatically — the Placeholder hides a missing/broken image on error.
const BASE = (typeof import.meta !== "undefined" && import.meta.env && import.meta.env.BASE_URL) || "/";
export const asset = (p)=> BASE + String(p).replace(/^\//,"");
export const catImg = (id)=> asset(`images/categories/${id}.jpg`);
// Main product photo lives at images/products/<SKU>.jpg (extra shots: <SKU>-2.jpg, <SKU>-3.jpg…).
// We key on the SKU/code — NOT the row id — because ids shift when the price list is rebuilt,
// while the SKU is stable, so a photo you add once keeps matching its product.
export const productImg = (key, n)=> asset(`images/products/${key}${n>1?"-"+n:""}.jpg`);
// gallery: only the images we actually have (no empty placeholder slots).
// p.image may hold one URL or several separated by spaces/commas/newlines.
export const productGallery = (p)=>{
  if(p.image){
    const urls = String(p.image).split(/[\s,]+/).filter(Boolean);
    if(urls.length) return urls;
  }
  return [productImg(p.code || p.key || p.id)]; // single slot (real local file or gradient)
};
export const HERO_IMG = [asset("images/hero-1.jpg"), asset("images/hero-2.jpg")];
export const ABOUT_IMG = asset("images/about.jpg");

// attach an image path to every category + product
CATS.forEach((c)=>{ c.img = catImg(c.id); });

// ---- Full catalog (generated from the spreadsheet via scripts/build-catalog.mjs) ----
import CATALOG_RAW from './catalog.json';
const _catMeta = Object.fromEntries(CATS.map(c=>[c.id, c]));
// normalise catalog rows into the same product shape the UI components expect
export const CATALOG = CATALOG_RAW.map(p=>{
  const c = _catMeta[p.cat];
  return {
    ...p,                               // includes stable `key` (SKU or hash fallback)
    ro:p.name, ru:p.nameRu || p.name,   // RU names generated in scripts/build-catalog.mjs
    badge: p.old>p.price ? "sale" : "",
    g: c ? c.g : ["#e7d6dd","#f5ebef"],
    icon: c ? c.icon : "bottle",
    img: p.image || productImg(p.code || p.key || p.id),  // supplier URL → local SKU file → gradient
  };
});

// ---- Lookups & helpers ----
// Live product set = real catalog only. Landing rows come from featured()/SALE_PRODUCTS.
export const ALL_PRODUCTS = CATALOG;

// products of a category (catalog only) + the brands available within it
export const productsByCat = (catId)=> CATALOG.filter(p=>p.cat===catId);
export function brandsByCat(catId){
  const counts = new Map();
  for(const p of productsByCat(catId)) counts.set(p.brand, (counts.get(p.brand)||0)+1);
  return [...counts.entries()]
    .sort((a,b)=> b[1]-a[1] || a[0].localeCompare(b[0]))
    .map(([brand,count])=>({brand,count}));
}

// products of a brand (catalog only) + the categories it spans
export const productsByBrand = (brand)=> CATALOG.filter(p=>p.brand===brand);
export function catsByBrand(brand){
  const counts = new Map();
  for(const p of productsByBrand(brand)) counts.set(p.cat, (counts.get(p.cat)||0)+1);
  return [...counts.entries()]
    .sort((a,b)=> b[1]-a[1])
    .map(([cat,count])=>({cat,count}));
}

// real brands from the price list, busiest first (excludes the unbranded bucket)
export const NO_BRAND = "Fără brand";
export const CATALOG_BRANDS = (()=>{
  const counts = new Map();
  for(const p of CATALOG) if(p.brand!==NO_BRAND) counts.set(p.brand,(counts.get(p.brand)||0)+1);
  return [...counts.entries()]
    .sort((a,b)=> b[1]-a[1] || a[0].localeCompare(b[0]))
    .map(([brand,count])=>({brand,count}));
})();

// stable, varied selection of real products for the landing rows.
// (The price list has no hit/new flags, so we spread real items across
//  categories deterministically — same picks on every load.)
const _hash = (x)=>{ let h=(x*2654435761)>>>0; h^=h>>>15; return (h*2246822519>>>0); };
export function featured(seed, n=8){
  const cats = [...new Set(CATALOG.map(p=>p.cat))];
  const out = [], used = new Set();
  for(let i=0; out.length<n && i<2000; i++){
    const pool = productsByCat(cats[(i+seed)%cats.length]);
    if(!pool.length) continue;
    const p = pool[_hash(seed*131+i) % pool.length];
    if(p && !used.has(p.id)){ used.add(p.id); out.push(p); }
  }
  return out;
}
export const SALE_PRODUCTS = CATALOG.filter(p=>p.old>p.price);
// look up by stable identity key (SKU or hash fallback) — used by URLs/cart/favorites
export const findProduct = (key)=> ALL_PRODUCTS.find(p=>p.key===key);

// stock comes from the sheet's Quantity column. Only an explicit 0 (or less) means
// out of stock; a blank/unknown quantity is treated as available (safe default).
export const inStock = (p)=> typeof p.stock !== "number" || p.stock > 0;
export const findCategory = (catId)=> CATS.find(c=>c.id===catId);

// real SKU from the price list when present, else a stable generated code
export const productCode = (p)=> p.code || "NM-"+String(p.id).padStart(4,"0");

// bilingual generated description (no real copy in the source data)
export function productDesc(p, lang){
  // catalog items carry a real description from the price list
  if(p.desc) return [p.desc, ""];
  const L = I18N[lang] || I18N.ro;
  const cat = findCategory(p.cat);
  const catName = cat ? (cat[lang]||cat.ro) : "";
  const a = (p[lang]||p.ro) + " " + L.descA.replace("{brand}", p.brand).replace("{cat}", catName);
  return [a, L.descB];
}

// ---- Product specs (Characteristics:* columns from the price list) ----
const SPEC_LABEL_RU = {
  "Cantitate":"Объём", "Putere":"Мощность", "Culoare":"Цвет", "Viteză maximă":"Макс. скорость",
};
const SPEC_COLOR_RU = {
  "Bej":"Бежевый", "Bej – roz":"Бежево-розовый", "Bej deschis":"Светло-бежевый",
  "Alb":"Белый", "Super alb":"Супер белый", "Grafit":"Графит", "Castaniu":"Каштановый",
  "Cafeniu":"Коричневый", "Cafeniu deschis":"Светло-коричневый", "Cafeniu inchis":"Тёмно-коричневый",
  "Roz":"Розовый", "Violet":"Фиолетовый", "Transparent":"Прозрачный", "Maro":"Коричневый",
  "Piersic":"Персиковый", "Laptisor":"Молочный", "Negru":"Чёрный", "Rosu":"Красный",
  "Cu sclipici":"С блёстками",
};
const specValueRu = (v)=>{
  if(SPEC_COLOR_RU[v]) return SPEC_COLOR_RU[v];
  if(/^da$/i.test(v)) return "Да";
  if(/^nu$/i.test(v)) return "Нет";
  return v.replace(/(\d)\s*ml\b/gi,"$1 мл").replace(/(\d)\s*W\b/g,"$1 Вт").replace(/rpm/gi,"об/мин");
};
// localized [{label, value}] for a product (RO as-is; RU labels + values translated)
export function productSpecs(p, lang){
  if(!p.specs || !p.specs.length) return [];
  if(lang!=="ru") return p.specs;
  return p.specs.map(s=>({ label: SPEC_LABEL_RU[s.label]||s.label, value: specValueRu(s.value) }));
}

// up to `n` other products sharing the category (falls back to bestsellers)
export function relatedProducts(p, n=4){
  const same = ALL_PRODUCTS.filter(x=>x.id!==p.id && x.cat===p.cat);
  const pool = same.length>=n ? same : [...same, ...ALL_PRODUCTS.filter(x=>x.id!==p.id && x.cat!==p.cat)];
  return pool.slice(0,n);
}
